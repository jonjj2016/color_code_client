import { autoTypeGen } from '../../Hooks/myTypes';
export const [types, actions] = autoTypeGen('modal', 'open', 'close');
// export const [types, actions] = autoTypeGen('palette', 'REGISTER', 'LOGIN', true);
